using FluentAssertions;
using Lighthouse.Core.Storage;
using System;
using Xunit;

namespace Lighthouse.Apps.WarehouseServer.Tests
{
    public class WarehouseServerTests
    {
        //[Fact]
        //public void Handle_WithStorageRequest_Retrieve_Returns_Successful()
        //{
        //    var mockWarehouse = NSubstitute.Substitute.For<IWarehouse>();
        //    WarehouseServer server = new WarehouseServer(mockWarehouse);

        //    var request = new StorageRequest
        //    {
        //        Action = StorageAction.Retrieve
        //    };

        //    var response = server.Handle(request);
        //    response.WasSuccessful.Should().BeTrue();
        //}

        //[Fact]
        //public void Handle_WithStorageRequest_Store_Returns_Successful()
        //{
        //    var mockWarehouse = NSubstitute.Substitute.For<IWarehouse>();
        //    WarehouseServer server = new WarehouseServer(mockWarehouse);

        //    var request = new StorageRequest
        //    {
        //        Action = StorageAction.Store
        //    };

        //    var response = server.Handle(request);
        //    response.WasSuccessful.Should().BeTrue();
        //}

        //[Fact]
        //public void Handle_WithStorageRequest_Inspect_Returns_Successful()
        //{
        //    var mockWarehouse = NSubstitute.Substitute.For<IWarehouse>();
        //    WarehouseServer server = new WarehouseServer(mockWarehouse);

        //    var request = new StorageRequest
        //    {
        //        Action = StorageAction.Inspect
        //    };

        //    var response = server.Handle(request);
        //    response.WasSuccessful.Should().BeTrue();
        //}

        //[Fact]
        //public void Handle_WithStorageRequest_Delete_Returns_Successful()
        //{
        //    var mockWarehouse = NSubstitute.Substitute.For<IWarehouse>();
        //    WarehouseServer server = new WarehouseServer(mockWarehouse);

        //    var request = new StorageRequest
        //    {
        //        Action = StorageAction.Delete
        //    };

        //    var response = server.Handle(request);
        //    response.WasSuccessful.Should().BeTrue();
        //}
    }
}
